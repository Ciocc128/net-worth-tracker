# CLAUDE.md - Net Worth Tracker (Lean)

## Project Overview
Next.js app for Italian investors: net worth, assets, cashflow, dividends, performance metrics and long-term planning on Firebase.

**This file is the INDEX**: "what it is + where it lives", nothing more — keep it well under 20.000 characters; it is injected into every turn. The *how* and the gotchas live in **AGENTS.md**; the aesthetic spec in **DESIGN.md**; env/emulators/Playwright in **SETUP.md**; users and positioning in **PRODUCT.md**. **Session rules and the guided-verification protocol are in [WORKFLOW.md](WORKFLOW.md); read it before starting.**

> **Language**: this file and AGENTS.md are in English. Italian is reserved for user-facing UI text. Page and feature names stay Italian, because they are the labels the product shows: Panoramica, Patrimonio, Cashflow, Analisi, Rendimenti, Allocazione, Storico, Previdenza, Impostazioni.

## Current Status
- Stack: Next.js 16, React 19, TypeScript 5, Tailwind v4, Firebase, Vitest, Framer Motion, Recharts, Yahoo Finance, Borsa Italiana scraping, Anthropic.
- `tsc` clean; **124 files / 2560 tests** green + **32 Playwright E2E specs** (35 green in one run incl. 3 auth setups). Run Vitest under `TZ=Europe/Rome` too — every date fixture sits at noon, which structurally hides timezone bugs.
- Latest (2026-08-25): **Storico al registro «Verdict over Tiles» — ottava pagina propagata.** Verdetto in `storicoNarrative.ts` («Il patrimonio è cresciuto, e sta accelerando.» / «…cresce al ritmo di sempre.» / «…ma ha rallentato.» / «…ma nell'ultimo anno ha perso.» / «…è sotto il punto di partenza.») su **«come sono arrivato qui?»**, SENZA asse: crescita di RICCHEZZA dal primo snapshot (versamenti inclusi, detto nella frase — mai il CAGR di Rendimenti), mese migliore, ultimo raddoppio, ultimo anno vs media. Griglia: Evoluzione 8×2 · Raddoppi 4×2 (UN ritmo — aumento medio mensile in € degli ultimi 12 mesi, lineare — per headline e prossimo raddoppio) · Composizione 8 (matematica intatta) · Driver 4 (risparmio vs mercato per anno dal floor del cashflow, sugli STESSI mesi della crescita, con quote) · Valore per strumento 12 (attribuzione prezzo/quantità per strumento); disclosure **Dettaglio** a quattro tessere. Numeri in `storicoSummary.ts`, parole in `storicoNarrative.ts` (80 test). Review avversaria (26 correzioni), collaudo 1440/390, screenshot README rifatto. Storia: `git log`.

## Architecture Snapshot
- App Router; protected pages under `app/dashboard/*`.
- `lib/services/*` (service layer) → pure `lib/utils/*` → `lib/server/*` (server-only). React Query for caching/invalidation.
- Italy timezone helpers in `lib/utils/dateHelpers.ts`.
- Convention: extract logic into pure, tested `lib/utils`/`lib/services` functions; keep Firestore-coupled code thin.

## Key Features (Active)
One line per feature: what it is, then where it is described. *What the user sees* → README.md; *the rules and the traps* → AGENTS.md; *the aesthetic* → DESIGN.md.

- **Shared account**: a second user as full co-owner; **viewer** (`user.uid`) ≠ **owner** (`ownerId`, `useActiveAccount()`), grants in `account-access/{ownerUid}`, enforced in `firestore.rules` + `assertCanAccessAccount`. AGENTS → *Shared Account / Delegated Access*.
- **Demo mode**: public landing with auto-login; `useDemoMode()` gates every mutation. AGENTS → *Demo Mode*.
- **Shell**: compact `PageHeader` (`legacy` only on Allocazione and Previdenza) · `PageTabBar` · `PageContainer width="wide"` + `TileGridSkeleton` for tile pages · sidebar with eyebrow group labels and the assistant as a route · bottom pill + «Altro» drawer. DESIGN → §5 Compact Page Header / Tile Grid; AGENTS → *Navigation*.
- **Panoramica**: a rule-generated verdict over a 12-column tile grid, one question per tile, on `GET /api/dashboard/overview`. README → *Portfolio Management*; AGENTS → *Panoramica and Dashboard Data Isolation*; DESIGN → §5 Page Verdict / Tile / Tile Grid.
- **Patrimonio**: the verdict's driver is an instrument; six tiles, Strumenti is the management table at the tile's cadence; a Δ is a unit-price variation. AGENTS → *Patrimonio*, *Asset Pricing, FX and Assets*.
- **AssetDialog + Asset trade ledger (Registro operazioni)**: 2-step create; BUY/SELL/ADJUSTMENT with cash settlement, Admin-API writes, the asset doc rebuilt by full replay. README → *Portfolio Management*; AGENTS → *Two-Step Create Dialogs*, *Asset Trade Ledger*.
- **Cashflow › Tracciamento**: «come sta andando il mese?» on one period axis; transfers are net-zero with atomic reconciliation; recurrence materialises real future rows. README → *Cashflow*; AGENTS → *Cashflow › Tracciamento*, *Expense Sign Convention*, *Recurring Series*.
- **Cashflow › Budget**: «sto rispettando il budget?», no axis, today's mark on every track, the ceiling historicised by the daily cron. AGENTS → *Cashflow › Budget*; DESIGN → Budget Track, Risk-vs-Fact.
- **Centri di Costo**: optional tab; «quanto sta costando il progetto?» with no axis — a project's cost is its whole cost. AGENTS → *Centri di Costo*; DESIGN → Whole-Cost Corollary.
- **Expense CSV Import**: preview-first, one-tap undo by `importBatchId`. AGENTS → *Expense CSV Import*.
- **Analisi**: «dove vanno i soldi, e cosa è cambiato?» on the three-mode axis; the focused entity is a tile; the app's only Sankey. README → *Cashflow*; AGENTS → *Analisi — a verdict over tiles*, *Expense Grouping*, *Sankey: node identity*.
- **Dividendi**: «quanto rendono i miei flussi?»; received and announced are never one figure; BTP Italia coupons. README → *Dividends*; AGENTS → *Cashflow › Dividendi*, *Dividends and Coupons*.
- **Rendimenti**: «quanto rende il portafoglio, e rispetto a cosa?» on one axis, a configurable base, six benchmarks in EUR. README → *Performance Analytics*; AGENTS → *Rendimenti — measurement base / the measurement window / a verdict over tiles*.
- **Storico**: «come sono arrivato qui?» with no axis — wealth growth (contributions included), ONE pace for the verdict and the next doubling, per-instrument price/quantity attribution. README → *Historical Analysis*; AGENTS → *Storico — a verdict over tiles*, *History and Snapshot Baselines*.
- **Allocazione**: a decision zone (hero, bands, action planner) then Dettaglio; `allocationRole` partitioned BEFORE `compareAllocations`; leverage as notional exposure. README → *Portfolio Management*; AGENTS → *Allocation — allocationRole*, *Allocation — the two plans and the leverage engine*.
- **Fondo Pensione (Previdenza)**: a price-1, frozen asset with three chapters on a year axis, multi-person, the fund's return kept apart from contributions. README → *Portfolio Management*; AGENTS → *Fondo Pensione*.
- **FIRE + Coast FIRE + What If + Monte Carlo**: a single-answer calculator; Coast is the same IA on «posso smettere di versare?»; the pension bridge model. README → *FIRE Planning*, *Monte Carlo Simulations*; AGENTS → *FIRE, What If and Goals*.
- **Goal-Based Investing**: trajectory-led goals; the Assistant proposes, only the user writes; SDK-free math in `goalMath.ts`. AGENTS → *FIRE, What If and Goals*, *Assistant*.
- **Assistente AI**: SSE streaming, five modes, gated web search, proactive memory, goals; flag `NEXT_PUBLIC_ASSISTANT_AI_ENABLED`, blocked in demo. AGENTS → *Assistant*.
- **Hall of Fame · Multi-theme · Periodic emails · PDF export**: README → *Other*; AGENTS → *Color Theme System*, *Periodic Emails*, *PDF Export*.

## Testing
- Vitest: `npx vitest run <file>`, `npm test -- <file>`, `npx tsc --noEmit`. Re-run `tsc` **after** writing the tests, not only after the code. New tests in `__tests__/`; prefer pure functions over Firestore-coupled code.
- **Phantom `tsc` errors**: `papaparse` and `@playwright/test` are declared but can be missing from the (untracked, branch-shared) `node_modules`. The tell is ~25 errors clustered in `e2e/` and `lib/utils/expenseImport.ts` rather than in what you touched — run `npm install` first.
- **Dev/test without production data**: Firebase Emulator Suite (`npm run emulators` + `emulators:seed` + `dev:emulator`), requires a JDK. SETUP.md → Step 6.
- **Browser (E2E)**: Playwright, `npm run test:e2e` with the emulators up (needs **Java ≥ 21**); app on :3100 with an isolated build dir. Covers only what needs a real layout; arithmetic stays with Vitest. Three accounts: base, **degraded** (`npm run e2e:seed -- suspicious|idle|fresh`) and the **Analisi fixture** (every expense dated January so figures survive any run month). SETUP.md → Step 7; gotchas: AGENTS.md → *Browser-Driven E2E*.

## Data & Integrations
Firestore client + admin · Yahoo Finance (prices, benchmark history) · Borsa Italiana scraping (Italian bonds, dividends) · Frankfurter (FX) · FRED (`FRED_API_KEY`, series ECBDFR) · Anthropic (`claude-sonnet-5` analysis + assistant, `claude-haiku-4-5` extraction).

## Known Issues (Active)
- **FX** depends on Frankfurter with a 24h in-memory cache (no fallback on a cold instance). Pre-migration non-EUR assets without `currentPriceEur` show the native price as EUR until the first update; one with `autoUpdatePrice: false` never self-heals until re-saved.
- **Demo account** requires manual Firebase setup — README.md → Known Issues + the three `NEXT_PUBLIC_DEMO_*` vars.
- **Two deliberate dependency pins keep advisories open.** `firebase-admin` at `^13.6.0` (@14 pulls pure-ESM `jose@6` → `ERR_REQUIRE_ESM` on Vercel; 8 moderate `uuid` advisories stay) and `next` at `~16.2.12` (16.3.0 breaks Vercel at `onBuildComplete`; 2 HIGH libvips advisories via `sharp`, low exposure). **Unpin next and re-run `npm audit fix` once Vercel digests 16.3.x.**
- **Shared account setup** prerequisites (whitelist, guest registers first, rules deployed): SETUP.md → Step 5b.
- **YOC/Current Yield** exclude sold assets and are scoped to the current holding via `holdingStartDate`; a sell+rebuy inside one month, or a gap in months without `byAsset`, counts the prior holding's dividends against the new cost basis (an overstated YOC, never a regression).
- **Rendimenti before `byAsset`: correct denominator, wrong numerator** (2023-01 → 2025-10 on the real account): the basis step is removed, but the excluded assets' variation stays inside the measured return. Not reconstructible.
- **TWR monthly-bucket artifact (by design)**: an expense is neutralised only when the net-worth drop and the cash flow land in the same month; recording a purchase both as an expense and as an asset produces a phantom gain. Record balances in the month they belong to.
- **The Assistant's cashflow figures changed on 2026-07-29**; saved threads are prose and are not regenerated.
- **Chart slots 5-9 are not theme-aware** (`useChartColors()` pads from the static `CHART_COLORS`): Obbligazioni vs Trend Following ΔE ≈ 8 on Storico → Composizione. Same root cause as the cost-center colour slots. Fix = one `--chart-6..8` pass across the 12 theme blocks.
- **Centri di Costo**: no Playwright spec of its own; `CostCenterDialog` keeps its pre-redesign chrome; an annual ceiling has no crossing day (`crossedOn` is monthly only); «Al mese» divides by the calendar months since the first expense (an idle project reads as a lower monthly cost, by design); «in totale» counts rows dated up to today (a backdated row moves total and crossing day, a future row is «in calendario» and not summed); the subcategory lens and the movements window (25 + «Mostra altre») are per-center, session-only state.
- **Sign-colour CHIPS sit below AA, structurally** (`bg-positive/10 text-positive` washes the background with the text's hue: 15 of 24 combinations at 3.34–4.40:1; deliberately not fixed). `MonthlyReturnsHeatmap` fills its cells with the sign tokens at 30/55/85% (the figure is never printed in the cell, so the AA text floor does not apply). Emails and the PDF are a permanent token-drift surface (literal hexes outside the DOM).
- **A confirmed goal proposal can be confirmed again after a reload** (accepted for v1): reopening the thread re-parses the fenced block and a second press creates a SECOND goal.
- **Tracciamento**: the «Tabella» view renders `ExpenseTable` unchanged inside the Movimenti tile; the period slice uses `periodToRange` (browser local time) while the month buckets use the Italian calendar; the phone bar's controls are 36px; `TransactionFeed`/`CompactExpenseRow` carry two pre-existing `react-hooks` errors. A custom range has no previous period; a running year is compared with the same months of the previous year; the month-end projection exists only in the current month; the running month is drawn but never ranked. `components/dashboard/overview/NarrativeText.tsx` is an unused re-export (knip).
- **Budget**: `BudgetItemDialog` stays for create/edit (no inline editing); **the ceiling history starts with the first cron run after the deploy** (earlier months read against today's ceiling, «prima quello attuale»), a month's record is its LAST captured configuration; the crossing day comes from the EXPENSE DATES (a backdated row moves it), an annual budget has no crossing sentence; a budget with every threshold off and already exceeded shows only in Per categoria. `app/dashboard/cashflow/page.tsx` carries two pre-existing `react-hooks` findings (`loadOtherData`).
- **Dividendi**: the payments table dropped *Tax/Netto/Costo per azione*; the calendar day opens the day dialog instead of filtering; under «Mese» no month arrows, under «Anno» they stop at January/December; the 2-click delete keeps its 3 s auto-disarm; the list toolbar is rendered twice. The yield never follows the period (TTM on the current holding); the DPS running-year column is a partial sum; no `averageCost` → the tile becomes an explanation.
- **Analisi**: «Fuori scala» runs on ONE month and is absent otherwise (thresholds 25% / 50 € over a 6-month average, hardcoded); a month not started yet gets the «non è ancora iniziato» verdict; «Mostra tutte», the Confronto's comparison year and the Flusso toggles are session-only; the Scheda's transactions window is 25 + «Mostra altre»; the `EntityDossier` trend stays Recharts; `SavingsRateTrendSection`/`AndamentoStoricoSection` compute their series in the component (untested); the Sankey drops small slices on phones (declared); no Playwright spec covers «Anno» with a month; `main` scrolls, so a 390 `fullPage` screenshot stops at the viewport.
- **Storico**: no Playwright spec of its own; the pace and the next-doubling projection need the snapshot of EXACTLY twelve months earlier (a gap drops both clauses) and the pace verdict 24 months of history; the projection is linear and dropped beyond fifty years; the Driver shows only the years from `cashflowHistoryStartYear` (a year's savings = the cashflow rows between its baseline snapshot and its last one; untracked income lands in «mercato») and its bars only the last twelve CALENDAR months; the Lavoro net can be negative on a positive gross (taxes estimated on ALL latent gains); the Evoluzione Y axis prints full amounts under 10.000 € of span; the confetti keeps literal hexes; the two dialogs keep their old chrome; at 768-1439 the skeleton stacks Raddoppi and Driver.
- **Two pages are not yet propagated** (Allocazione, Previdenza; Centri di Costo is a tab and keeps its shape): they carry `PageHeader variant="legacy"` and a per-page skeleton.
- **Rendimenti**: no Playwright spec of its own; the six benchmark series + FX load on every visit (6h `staleTime`), only a FAILED FX route falls back to USD (the aside says so); Sharpe/Sortino use the settings' risk-free rate (the ECB period rate went with the old table); the payload's index-step `drawdownDuration`/`recoveryTime` are no longer displayed (the tiles read `resolveDrawdownStory`, calendar months); a 1-anno window without the current month's snapshot measures 11 months and says so; the rolling readings are built in `PerformanceDettaglio` (untested); `AIAnalysisDialog`/`CustomDateRangeDialog` keep their old chrome.
- **Patrimonio**: Δ columns are empty for pension funds and cash accounts by design; the Rendimento tile ranks only within the overview's `topAssets` (15 largest); «Movimenti del mese» reads the whole ledger and filters in memory (no `(userId, date)` index); the 2-click delete auto-disarms on a 3 s timer (kept on request); G/P against PMC compares a native-currency `averageCost` with the EUR value; `TaxCalculatorModal` simulates in the native price but labels €; the Compra/Vendi chips use the sign tokens for a trade direction. `AssetDialog.tsx` carries 7 pre-existing `react-hooks` errors.
- **The icon rail's 44px targets are measured at 1440 with a mouse**; no fixture covers a ≥1440px tablet in landscape.
- **The market digest's blind spots**: a position opened this month contributes 0 until next month; a pension fund counts only from `pensionReturnStartMonth`; hand-valued assets other than funds and real estate never show a market effect; real estate is measured gross of debt. Honest by construction.
- **A class held without a target entry is invisible to the balance score** (`toLegacyAllocationResult` iterates `Object.keys(targets)`).

## Key Files
Tests are not listed: every pure module has `__tests__/{module}.test.ts`, every page its `e2e/{page}*.spec.ts` where one exists.
- **Shell**: `app/dashboard/layout.tsx`, `components/layout/{Sidebar,BottomNavigation,SecondaryMenuDrawer,PageHeader,PageTabBar,PageTabs,PageContainer,ThemePicker,LogoutDialog}.tsx` + `shellStyles.ts` (`SIDEBAR_AVATAR_CLASS`), `components/ui/sidebar.tsx` (`SIDEBAR_WIDTH_ICON` = 3.5rem, icon-mode button size), `lib/constants/navigation.ts` (`primaryNav`/`analysisNav`/`planningNav`/`assistantNavItem`/`secondaryHrefs`), `lib/utils/navUtils.ts`; tile primitives `components/ui/{tile,narrative-text,ranked-rows,tile-grid-skeleton,page-verdict}.tsx`, `lib/utils/{narrative,tileGridSkeleton}.ts` (`narrative.ts` owns `Narrative`, `VerdictTone`, `PageVerdictModel`)
- **Patrimonio**: `app/dashboard/assets/page.tsx` (owns every dialog), `components/assets/{StrumentiTile,AssetRow,CashAccountDialog,AssetDialog,TransactionDialog,AssetMovementsDialog,TaxCalculatorModal,AssetSparkline}.tsx`, `components/assets/tiles/{LiquiditaTile,MovimentiTile,RendimentoTile}.tsx` (+ `PatrimonioTile`/`ComposizioneTile` reused from `components/dashboard/overview/` with `movers`/`countLine`/`eyebrow`/`footer` props); pure `lib/utils/{patrimonioNarrative,patrimonioSummary,assetPerformanceDeltas}.ts`; payload `topInstrumentMovers` via `computeTopInstrumentMovers` (`dashboardOverviewUtils.ts`)
- **Overview**: `app/dashboard/page.tsx`, `app/api/dashboard/overview/route.ts`, `lib/services/dashboardOverviewService.ts`, `lib/hooks/useDashboardOverview.ts`, `components/dashboard/overview/*` (`OverviewTile`/`NarrativeText`/`RankedRows`/`OverviewVerdict` re-exports or wrappers of the `components/ui` primitives, one file per tile; `PatrimonioTile` exports `resolveHeroValueClass`), `components/dashboard/{OverviewAnimatedCurrency,NetWorthSparkline,PeriodSelector}.tsx`, `lib/utils/{overviewNarrative,dashboardOverviewUtils,sparklinePeriod,savingsRateBadge}.ts`
- **Shared account**: `contexts/ActiveAccountContext.tsx`, `lib/services/accountAccessService.ts`, `app/api/account/members/route.ts`, `lib/server/apiAuth.ts`, `firestore.rules`, `components/settings/AccountSharingSection.tsx`; collection `account-access/{ownerUid}`
- **Shared primitives**: `components/ui/{composition-list,composition-bar,segmented-pill,drill-breadcrumb,responsive-modal,chart-hover}.tsx`, `lib/utils/compositionShading.ts`
- **Shared utils** (each the single source of its rule): `lib/utils/formatters.ts` · `metricColors.ts` (`getMetricValueColor`) · `assetPricing.ts` (`requiresManualPricing`) · `assetLiquidity.ts` (`suggestIsLiquid`) · `expenseTypeTransition.ts` (`needsSignFlip`/`crossesTransferBoundary`) · `firestoreData.ts` (`removeUndefinedDeep`) · `dateHelpers.ts` (`endOfMonthBound`, `getItalyDateIso`)
- **Rendimenti**: `app/dashboard/performance/page.tsx`, `components/performance/tiles/*` (seven tiles), `components/performance/{GrowthOfHundredChart,CapitalMarketChart,MonthlyReturnsHeatmap,PerformancePeriodPicker,PerformanceDettaglio,UnderwaterDrawdownChart,CustomDateRangeDialog,AIAnalysisDialog}.tsx`; pure `lib/utils/{performanceNarrative,performanceSummary}.ts` (words · numbers), `performanceBase.ts`, `drawdownSeries.ts`, `lib/services/performanceService.ts` (window, IRR, `CACHE_MATH_VERSION`), `cashFlowMap.ts`, `benchmarkPeriodReturn.ts`. Cache `performance-cache/{userId}`
- **Yields**: `lib/utils/yieldOnCost.ts` (`computeDividendYieldMetrics` — single source), consumed by Rendimenti and `app/api/dividends/stats/route.ts`
- **Assets**: `lib/services/assetService.ts` (`createAsset` ISIN reuse + `holdingStartDate`), `components/assets/*`, `types/assets.ts`, `lib/utils/{assetDisplay,assetDisplayClass}.ts`
- **Asset trade ledger**: engine `lib/utils/assetTransactionUtils.ts` + `types/assetTransactions.ts`; server `lib/server/{assetTransactionUseCase,tradeFxService}.ts` + `app/api/asset-transactions/*`; client `lib/services/assetTransactionService.ts` + `lib/hooks/useAssetTransactions.ts`; UI `components/assets/{TransactionDialog,AssetMovementsDialog}.tsx`. Collections `assetTransactions`/`assetTransactionsMeta`
- **Fondo pensione**: `types/pension.ts` + `pensionFundDetails`/`FamilyMember` (`types/assets.ts`); pure `lib/utils/{pensionDeduction,pensionContributions,pensionReturn,pensionFire,pensionFamilyMembers}.ts`; `lib/services/pensionContributionService.ts` (`assertFundValueLivesInQuantity` = the price-1 guard); UI `components/pension/*` + `app/dashboard/pension/page.tsx`. Collection `pensionContributions`
- **E2E**: `playwright.config.ts`, `e2e/*.ts`, fixtures `scripts/{seedPensionE2E,seedAnalisiE2E,seedCoastFireE2E}.mts`; scripts `test:e2e`/`e2e:seed`/`e2e:seed:analisi`/`e2e:seed:coast`/`dev:e2e`
- **Allocazione / exposure**: `app/dashboard/allocation/page.tsx`, `components/allocation/*`, `lib/utils/{allocationUtils,leverageAwareAllocationUtils,assetExposureUtils}.ts` (`allocationUtils` also owns `ASSET_CLASS_SEQUENCE` = the one enumeration of the union, `ASSET_CLASS_LABELS` and `ASSET_CLASS_CHART_INDEX` = the one source of per-class chart slots), `lib/services/assetAllocationService.ts`, `lib/hooks/useActionColors.ts`; `lib/server/portfolioExposureService.ts`, `exposure-cache/{userId}`
- **Cashflow › Tracciamento**: `components/cashflow/ExpenseTrackingTab.tsx` (state, handlers, the grid; `applyListFilters` at module level), `components/cashflow/tiles/{CashflowPeriodoTile,FlowBarsChart,RisparmioTile,MovimentiTile}.tsx` (+ `CategoryTile` from the overview with `reading`), `components/cashflow/{TransactionFeed,CompactExpenseRow,MobileFiltersDrawer}.tsx`; pure `lib/utils/{tracciamentoSummary,cashflowNarrative}.ts`
- **Cashflow › Budget**: `components/cashflow/BudgetTab.tsx` (state, grid, the `cashflow:add-budget` listener), `components/cashflow/budget/tiles/*`, `components/cashflow/budget/{BudgetTrack,BudgetImpostazioni,BudgetItemDialog,budgetProgressStyle,useArmedDelete}.ts(x)`; pure `lib/utils/{budgetSummary,budgetNarrative,budgetUtils,budgetHistory,spendingProjection}.ts` (`spendingProjection` = the ONE month-end projection, shared with the Panoramica, Tracciamento and both budget emails; `budgetHistory` = the record builder + `resolveMonthCeilings`); `lib/hooks/{useBudgetConfig,useBudgetHistory}.ts`, `lib/services/budgetHistoryService.ts` (client), `lib/server/budgetHistoryService.ts` (cron phase 8); collections `budgets/{userId}`, `budgetHistory/{userId}/months/{YYYY-MM}`
- **Cashflow / cost centers**: `app/dashboard/cashflow/page.tsx`, `components/cashflow/*`, `lib/utils/{costCenterUtils,cashflowTimeSeries,expenseImport,recurrenceDates}.ts` (`recurrenceDates` = the ONE source on recurrence dates, ceilings and eligible types), `lib/services/{budgetService,costCenterService,cashBalanceReconciliation,expenseImportService}.ts`
- **Centri di Costo**: `components/cashflow/{CostCentersTab,CostCenterDetail,CostCenterDialog,CostCenterErrorNotice}.tsx` (the tab owns query, dialog and the `cashflow:add-cost-center` listener), `components/cashflow/cost-centers/{CenterChip,ArchiviatiDisclosure}.tsx` + `cost-centers/tiles/*` (`CenterStackBars`); `costCenterStyles.ts` (`CHART_TICK_STYLE` — the canonical Recharts tick style); pure `lib/utils/{costCenterSummary,costCenterNarrative,costCenterUtils,costCenterColors}.ts` (numbers · words · compositions + unscoped dormancy · slot resolution), `lib/utils/spendingProjection.ts` (`projectWindowEndWithScheduled` = the ONE rule on any window); `lib/services/costCenterService.ts`
- **Analisi**: pure `lib/utils/{expenseGrouping,cashflowSankey,cashflowComposition,expenseCategoryMatching,comparisonDeltas,expenseEntityStats,entitySearch}.ts` + page layer `lib/utils/{analisiSummary,analisiNarrative}.ts`; UI `components/cashflow/AnalisiTab.tsx` (state, URL, grid, `handleEntitySelect`), `components/cashflow/analisi/*` (+ `tiles/*`), `components/cashflow/{EntityDossier,EntitySearch,ConfrontoAnnualeSection,CashflowSankeyChart,SavingsRateTrendSection,AndamentoStoricoSection}.tsx`, `components/cashflow/tiles/CashflowKpiTrio.tsx` (shared with Tracciamento)
- **Cashflow › Dividendi**: `components/dividends/DividendTrackingTab.tsx` (state, filters, the grid), `components/dividends/tiles/{IncassoNettoTile,AffidabilitaTile,RendimentoTile,PagatoriTile,PerAnnoTile,PagamentiTile,NetIncomeBars}.tsx`, `components/dividends/DividendiDettaglio.tsx`; pure `lib/utils/{dividendAnalytics,dividendiNarrative}.ts`; server block `lib/hooks/useDividendStats.ts` → `app/api/dividends/stats/route.ts` (payload type `DividendStatsPayload` in `types/dividend.ts`)
- **Dividendi (registro e cedole)**: `components/dividends/{DividendTable,DividendCalendar,CalendarDayCell,DividendDialog,DividendDetailsDialog,DividendRecordDetailsDialog,InflationRateDialog,ProvisionalCouponBanner}.tsx`, `lib/utils/couponUtils.ts`, `lib/services/couponScheduling.ts`, `types/dividend.ts`
- **Storico / snapshots**: `app/dashboard/history/page.tsx`, `components/history/tiles/*` (five tiles), `components/history/{StoricoDettaglio,SnapshotSearchDialog}.tsx`; pure `lib/utils/{storicoSummary,storicoNarrative}.ts` (numbers · words), `lib/utils/{snapshotAssetBreakdown,historyComposition}.ts` (`buildMonthAssetBreakdown`/`summarizeSelection` = the per-instrument reading; `historyComposition` = the ONE source on bands, normalization, residual and Δpp), `lib/services/{chartService,snapshotService}.ts` (`prepareDoublingTimeData`, `prepareSavingsVsInvestmentData*`, `prepareAssetClassHistoryData` returns EURO, never shares); `components/ui/aside-toggle.tsx` (shared with Analisi); collection `monthly-snapshots`
- **Benchmark**: `lib/constants/benchmarks.ts`, `app/api/benchmarks/*`, `lib/server/ecbRatesService.ts`; caches `benchmark-cache/*`, `fx-rate-cache/usd-eur`, `ecb-rate-cache/deposit-rate`
- **FIRE / goals**: `components/fire-simulations/*` (`FireFanChart` = the Ventaglio; `FIREProjectionSection` = presentation only), `lib/services/{fireService,whatIfService,monteCarloService,goalService}.ts`, `lib/utils/{goalTrajectory,goalMath,pensionUnlock,monteCarloParams}.ts` (`pensionUnlock` = the single unlock resolution; `deriveMonteCarloAllocation` = the ONE allocation→MC-classes normalizer; `goalMath` = SDK-free math + `serializeGoalForFirestore`, the persistence allowlist), `components/goals/*`
- **Coast FIRE**: `components/fire-simulations/CoastFireTab.tsx` (orchestrator) + `coast/*` + `CoastFireProjectionChart.tsx`; pure view layer `lib/utils/coastFireView.ts` (**no math**), form state `lib/hooks/useCoastFireSettingsDraft.ts`. Fixture `scripts/seedCoastFireE2E.mts`
- **Assistant**: `app/dashboard/assistant/page.tsx`, `components/assistant/*`, `app/api/ai/assistant/*`, `lib/server/assistant/*`; streaming `lib/hooks/useAssistantStreaming.ts`; period axis `lib/utils/assistantPeriodOptions.ts`; context `lib/services/assistantMonthContextService.ts` (4 period builders) over `lib/utils/expenseBreakdown.ts` (`buildCashflowBreakdown` — single aggregator)
- **Assistant — obiettivi**: pure `lib/server/assistant/goalEvaluation.ts`, I/O `goalEvaluationService.ts` (`evaluateActiveGoals`), extraction `memoryExtraction.ts`; `updatedAt` semantics in `store.ts` → `mergeMemoryItem`
- **Assistant — Goal-Based Investing**: `lib/server/goalData.ts`, `lib/utils/goalProposal.ts` (ONE zod schema for the fenced block and the route body), `app/api/goals/route.ts`, `components/assistant/GoalProposalCard.tsx` + the `pre` interception in `AssistantStreamingResponse.tsx`
- **Settings / layout**: `app/dashboard/settings/page.tsx`, `lib/services/assetAllocationService.ts` (`calculateEquityPercentage`), `lib/utils/equityBondsAutoTargets.ts`, `components/layout/*`, `lib/constants/navigation.ts`
- **Server use case / email**: `lib/server/{assetAdminRepository,dividendUseCase,dividendProcessor,monthlyEmailService,weeklyBudgetEmailService,emailPeriodComparison}.ts`, `app/api/cron/monthly-snapshot/route.ts` (phases 2-7)
- **PDF export**: `lib/utils/pdfGenerator.tsx` → `lib/services/pdfDataService.ts` → `components/pdf/*`; `lib/utils/pdfTimeFilters.ts`; types `types/pdf.ts`

## Design Context
Propagation plan: **`docs/redesign-prompts.md`** — one ready-to-paste prompt per section (shell, every page/tab, auth, landing, dialogs, states, email/PDF) with the recommended model/effort and the closing checklist.

Authoritative aesthetic spec: **DESIGN.md** (Apple + Linear/Vercel + Trade Republic; form-follows-function) — hand-maintained, **never regenerate it**; its YAML frontmatter is the normative layer read by the impeccable detector, `.impeccable/design.json` is only the extensions sidecar. Product truth lives in **PRODUCT.md**. Principles: (1) data first, decoration never; (2) motion with a purpose; (3) density is a feature; (4) precision builds trust; (5) personality lives in the details.

**Last updated**: 2026-08-25 (DESIGN.md documents the "Verdict over Tiles" shape, the shell and the first eight propagations — Patrimonio, Tracciamento, Dividendi, Budget, Centri di Costo, Analisi, Rendimenti, Storico — each with the rule it added (Table/Feed/Heatmap inside a Tile, Off-Axis Tile, Received-vs-Announced, Budget Track, Risk-vs-Fact, Whole-Cost Corollary, Scheda inside the Grid, Same-Basis, One-Pace Corollary); superseded patterns are marked, not deleted, until every page is propagated). History: `git log`.
